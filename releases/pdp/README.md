# PDP releases

Each JSON manifest in this directory records one shared PDP section release or one explicitly approved product exception.

Use the workflow in `.agents/skills/roman-pdp-rollout/references/release-workflow.md`. Do not combine multiple sections in one manifest, and do not include alternate product templates in an `all-pdps` release.
